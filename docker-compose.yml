version: '3.9'

services:
#    app:
#        build: .
#        ports:
#            - "8080:8080"
#        volumes:
#            - .:/app
    redis:
        image: redis:7.4-alpine
        ports:
            - "6379:6379"
        volumes:
            - ./var/redis:/data