version: '3.9'

services:
#    app:
#        build: .
#        ports:
#            - "3000:3000"
#        volumes:
#            - .:/app
    redis:
        image: redis:7.4-alpine
        ports:
            - "6379:6379"
        volumes:
            - ./var/redis:/data